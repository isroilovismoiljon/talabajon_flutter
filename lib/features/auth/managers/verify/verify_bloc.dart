import 'package:flutter/material.dart';

class VerifyBloc extends StatelessWidget {
  const VerifyBloc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
